<manifest
	xmlns:android="http://schemas.android.com/apk/res/android"
	package="ejercicio.app_m">

	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

	<application
		android:allowBackup="true"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:supportsRtl="true"
		android:theme="@style/AppTheme">
	
		<activity
			android:label="@string/initial_activity_title"
			android:name="ejercicio.app_m.activity.InitialActivity"
			android:theme="@style/InitialActivityTheme">
		
			<intent-filter>
			
				<category android:name="android.intent.category.LAUNCHER"/>
			
				<action android:name="android.intent.action.MAIN"/>
			</intent-filter>
		</activity>
	
		<activity
			android:label="@string/item_specs_activity_title"
			android:name="ejercicio.app_m.activity.ItemSpecsActivity"
			android:theme="@style/ItemSpecsActivityTheme"/>
	
		<activity
			android:label="@string/results_activity_title"
			android:name="ejercicio.app_m.activity.ResultsActivity"
			android:theme="@style/ResultsActivityTheme"/>
	</application>
</manifest>